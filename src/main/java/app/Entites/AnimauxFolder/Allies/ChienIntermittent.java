package app.Entites.AnimauxFolder.Allies;

public class ChienIntermittent extends ChatClassique {
    public ChienIntermittent(double x, double y, double health, double vitesse, double dmg, double range) {
        super(x, y, health, vitesse, dmg, range);
    }
}
